begin
  require 'bundler'
rescue LoadError
  puts 'You must `gem install bundler` and `bundle install` to run rake tasks'
end

ALL_HOOKS = %w{
  
}

namespace :hookspec do
  task :create do |t|
    
  end
end
